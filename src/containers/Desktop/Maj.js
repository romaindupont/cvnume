import { connect } from 'react-redux';
// == Import : local
import Maj from '../../components/Desktop/Maj';


const mapStateToProps = null;

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Maj);